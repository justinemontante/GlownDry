# Agent Preferences

- Auto-push to `origin/main` after every commit.
- Use explicit `git add <path>` (never `git add -A` or `git add .`) — `nul` file at workspace root breaks glob adds.
- Taglish labels preferred ("Drop off Schedule" instead of "Schedule Date").
- No riders — customer drops off laundry, so schedule = drop-off time.
